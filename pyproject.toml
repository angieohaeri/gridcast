[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "gridcast"
version = "0.0.1"
description = "Short-term electricity load forecasting for PJM zones."
authors = [
  { name = "Angie Ohaeri" },
]
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.12"
dependencies = [
    "loguru",
    "matplotlib",
    "mlflow",
    "numpy",
    "pandas",
    "python-dotenv",
    "scikit-learn",
    "lightgbm",
    "tqdm",
    "typer",
    "dvc",
    "gridstatus>=0.36.0",
]

[project.optional-dependencies]
dev = [
    "ipython",
    "jupyterlab",
    "notebook",
    "pre-commit",
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 99
src = ["gridcast"]
include = ["pyproject.toml", "gridcast/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]

[tool.ruff.lint.isort]
known-first-party = ["gridcast"]
force-sort-within-sections = true

[dependency-groups]
dev = [
    "pre-commit>=4.6.1",
]
